version: 2

macros:
  - name: postgres__create_table_as
    description: creates a columnar table in postgres # it not works by default in this rdbms
    arguments:
      - name: temporary
      - name: relation
      - name: sql