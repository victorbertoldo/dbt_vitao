version: 2

macros:
  - name: uindex
    description: >
      LEGACY — Creates a unique index on a single column. Use dbt_vitao.optimize_relation() for new projects.
    arguments:
      - name: this
        description: Target relation.
      - name: column
        description: Column name.
